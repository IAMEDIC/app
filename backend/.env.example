# IAMEDIC Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# === Application Settings ===
APP_NAME=IAMEDIC
DEBUG=true
ENVIRONMENT=development
VERSION=1.0.0
HOST=0.0.0.0
PORT=8000

# === Database Configuration ===
DATABASE_URL=postgresql://iamedic_user:secure_password@postgres:5432/iamedic_db

# PostgreSQL specific settings
POSTGRES_DB=iamedic_db
POSTGRES_USER=iamedic_user
POSTGRES_PASSWORD=secure_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# === Security ===
SECRET_KEY=your_secret_key_here_minimum_32_characters
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_32_characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === Redis Configuration ===
REDIS_URL=redis://redis:6379/0
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# === Google OAuth Configuration ===
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Create OAuth 2.0 credentials and set authorized redirect URIs
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback

# === CORS Configuration ===
# Frontend URLs that can access the backend
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://frontend:3000

# === MLFlow Configuration ===
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_URI=http://host.docker.internal:8080

# === Admin Configuration ===
# Initial admin user - this user will be automatically assigned admin role
INIT_ADMIN_EMAIL=iamedicsa@gmail.com

# === Frame Classifier Service ===
FRAME_CLASSIFIER_URL=http://frame-classifier-service:8001

# === BloodFlow Biometrics Registration Service ===
BB_REG_SERVICE_URL=http://bb-reg-service:8002

# === Production-specific Settings ===
# For production deployment, update these values:
# DATABASE_URL=postgresql://user:password@your_production_db_host:5432/iamedic_db
# REDIS_URL=redis://your_production_redis_host:6379/0
# FRONTEND_URL=https://your-production-domain.com
# GOOGLE_REDIRECT_URI=https://your-production-domain.com/auth/callback
# DEBUG=false
# ENVIRONMENT=production
